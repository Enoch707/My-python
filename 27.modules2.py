#lets create a module
#go to module.py to see what we did to achieve it
import circle#the name of the file that created the module with
print(circle.perimeter(3))#the name of the file (cirle).one of the functions(perimeter in this case)
print(circle.simplepie)#the variable
#this is why def is so important because its the functions you will use to create modules
#tho types like time 
# i dont know how that one is replicated
print(circle.cone(3,5))
