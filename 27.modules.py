#modules aka python library do u remember import random or import time does are modules 
# they are a bunch of codes that can be imported for specific needs
# # you can also create yours like def for example;
# help('modules')#to see the availaible list of modules     
# while True:
#     mod=input('What module are u trying to see')
#     print(help(mod))#this helps ulook at each help                            
#example
import math as m#i have access to math or if u just want it to be math its import math
print(m.pi)#anytime i want to use any command from the math module u will use it as the thing u named is as in this case m
print(m.e)
from math import pi #just to access the pie function
rad=float(input('Radius: '))
print(2*pi*rad)#in this case u dont have to type math.
